// Generated by CoffeeScript 1.7.1
(function() {
  var coffeeRef, myFirebaseRef, recentFindsref;

  myFirebaseRef = new Firebase("https://dandenney.firebaseio.com/");

  recentFindsref = new Firebase("https://dandenney.firebaseio.com/recentFinds");

  $("#findSubmit").click(function() {
    var findDescription, findTitle, findUrl;
    findTitle = $("#findTitle").val();
    findUrl = $("#findUrl").val();
    findDescription = $("#findDescription").val();
    return recentFindsref.push({
      title: findTitle,
      url: findUrl,
      description: findDescription
    });
  });

  $(function() {
    return recentFindsref.on('child_added', function(snapshot) {
      var description, title, url;
      url = snapshot.val().url;
      title = snapshot.val().title;
      description = snapshot.val().description;
      return $("#recentFinds").append("<li>" + "<h3>" + "<a href='" + url + "'>" + title + "</a>" + "</h3>" + "<p>" + description + "</p>" + "</li>");
    });
  });

  coffeeRef = new Firebase("https://dandenney.firebaseio.com/coffee");

  $("#coffeeCount").change(function() {
    var coffeeCount, coffeeDate;
    coffeeCount = $(this).val();
    coffeeDate = $('#coffeeDate').val();
    return coffeeRef.update({
      date: coffeeDate,
      count: coffeeCount
    });
  });

}).call(this);
